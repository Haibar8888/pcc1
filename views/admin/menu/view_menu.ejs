<%- include('../../partials/header') %>
<!-- Page Wrapper -->
<div id="wrapper">
    <!-- Sidebar -->
    <%-include('../../partials/sidebar') %>
    <!-- End of Sidebar -->

    <!-- Content Wrapper -->
    <div id="content-wrapper" class="d-flex flex-column">
        <!-- Main Content -->
        <div id="content">
            <!-- Topbar -->
            <%- include('../../partials/navbar') %>
            <!-- End of Topbar -->

            <!-- Begin Page Content -->
            <div class="container-fluid">
                <%- include('../../partials/message') %>
                <!-- Page Heading -->
                <%- include('view_table_menu') %>
            </div>
            <!-- /.container-fluid -->
        </div>
        <!-- End of Main Content -->

        <!-- Footer -->
        <%- include('../../partials/footer') %>
        <!-- End of Footer -->
    </div>
    <!-- End of Content Wrapper -->
</div>
<!-- End of Page Wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
</a>

<!-- Logout Modal-->
<div
    class="modal fade"
    id="logoutModal"
    tabindex="-1"
    role="dialog"
    aria-labelledby="exampleModalLabel"
    aria-hidden="true"
>
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">
                    Ready to Leave?
                </h5>
                <button
                    class="close"
                    type="button"
                    data-dismiss="modal"
                    aria-label="Close"
                >
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">
                Select "Logout" below if you are ready to end your current
                session.
            </div>
            <div class="modal-footer">
                <button
                    class="btn btn-secondary"
                    type="button"
                    data-dismiss="modal"
                >
                    Cancel
                </button>
                <a class="btn btn-primary" href="login.html">Logout</a>
            </div>
        </div>
    </div>
</div>
<%- include('../../partials/js') %> <%- include('add_menu') %>

<script>
    $('#tableBank').on('click', '.button-edit', function() {
        let id = $(this).data('id');
        let name = $(this).data('name');
        let namak = $(this).data('namak');
        let kategoriId = $(this).data('kategori');
        let namas = $(this).data('namas');
        let subkategoriId = $(this).data('subkategori');
        let namal = $(this).data('namal');
        let lokasiId = $(this).data('lokasi');
        let qty = $(this).data('qty');

        $('#edit-modal').modal('show');
        $('.id').val(id);
        $('.name').val(name);
        $('.kategori').html(namak);
        $('.kategori').val(kategoriId);
        $('.subkategori').html(namas);
        $('.subkategori').val(subkategoriId);
        $('.lokasi').html(namal);
        $('.lokasi').val(lokasiId);
        $('.qty').val(qty);
    });

    function tambahInput() {
        $('#i2').toggle();
        $('#i2').keyup(function() {
            let qty = parseInt($('#qty').val());
            let tambah = parseInt($('#i2').val());
            let total = qty + tambah;
            let hasil = $('#hasil').val(total);
            console.log(hasil.val());
        });
        $('#keterangan').val('IN');
    }
    function stockAwal() {
        $('#input').toggle();
    }
    function kurangiInput() {
        $('#r3').toggle();
        $('#r3').keyup(function() {
            let qty = parseInt($('#qty').val());
            let tambah = parseInt($('#r3').val());
            let total = qty - tambah;
            let hasil = $('#hasil').val(total);
            console.log(hasil.val());
        });
        $('#keterangan').val('OUT');
    }
    $(document).ready(function() {
        $('#tableBank').DataTable({});
        $('.input').hide();
        $('.i2').hide();
        $('.r3').hide();
    });
</script>
